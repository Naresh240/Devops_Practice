  sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
  dnf install docker-ce-3:18.09.1-3.el7
  systemctl enable docker
  systemctl start docker
  systemctl enable docker
  docker pull tomcat
  docker images
  docker run --name=tomcatletest -p 8080:8080 -d tomcat:latest
  docker ps
  docker stop d6ecc2754fc6
  docker ps
  docker ps -a
  docker rm d6ecc2754fc6
  docker ps -a
  docker images
  dockr rmi
  docker images
  docker rmi
